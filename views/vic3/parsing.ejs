<%- include('../partials/head') %>

    Parsing save. It'll take no longer than a minute. Please, refresh the page if nothing happens.

    <script defer>
        setTimeout(function () {
            window.location = window.location;
        }, 5 * 1000);
    </script>

    <%- include('../partials/bottom') %>